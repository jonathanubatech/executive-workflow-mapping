# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) and other AI assistants when working with this repository.

## Project Overview

**`executive-workflow-mapping`** — A structured framework for mapping, diagnosing, and redesigning executive workflows across enterprise tool stacks (M365, Jira, Confluence, Slack/Teams).

- **Owner:** `jlynshue`
- **Primary language:** `mixed`
- **Default branch:** `main`
- **Canonical remote:** `https://github.com/jlynshue/executive-workflow-mapping`

## Development Commands

> Fill in this section with real commands for this repo. Templates below are starting points — replace with actual commands after first use.

```bash
# Install dependencies
# TODO: fill in install command

# Run tests
# TODO: fill in test command

# Lint / format
# TODO: fill in lint command

# Build / dev server
# TODO: fill in build command
```

## Architecture

> Document the structure once — when the code tells a reader what each layer does via well-named files, a short paragraph here is enough.

> TODO: describe architecture.

## Code Style

Follow the conventions of the primary language:

- **Python:** Black (88 chars), type hints required.
- **JavaScript / TypeScript:** npm only (never yarn), ESLint + TypeScript strict mode.
- **Naming:** Directories = `kebab-case`, Python = `snake_case.py`, JS/TS = `camelCase.ts`.

## Testing

> Fill in how tests are organized, test markers, coverage expectations, and how to run a single test.

## Deployment

> Fill in deployment targets, release process, and rollback procedure. Every deployment requires QA and regression testing before pushing.

## History

See [`docs/HISTORY.md`](docs/HISTORY.md) for merged PRs, baseline tags, and major decisions.

## AI Assistant Rules

See [`AGENTS.md`](AGENTS.md) for guardrails that apply to all AI coding assistants.
